"VertexLitGeneric"
{
    "$basetexture" "${EXPORTPATH}\${ALBEDONAME}"
    "$bumpmap" "${EXPORTPATH}\${NORMALNAME}"
    "$phongexponenttexture" "${EXPORTPATH}\${EXPONENTNAME}"
       
    /////////////////
    "$color2" "[0 0 0]"                                     //do not touch this
    "$blendtintbybasealpha" "1"                             //do not touch this
    /////////////////

    "$phong" "1"
    "$phongboost" "${PHONGBOOST}"
    "$phongalbedotint" "1"
    "$phongfresnelranges" "[1 0.5 0]"

    //rimlight doesn't properly show in hlmv, make sure you're changing these values in game
    "$rimlight" "1"    "$rimlightexponent" "100"
    "$rimmask" "1"
    "$rimlightboost" "1"

    /////////////////
    "$normalmapalphaenvmapmask" "1"                         //do not touch this
    "$envmapfresnel" "1"                                    //do not touch this
    /////////////////

    "$envmap" "env_cubemap"
    "$envmaptint" "[0.2 0.2 0.2]"

    //remove this if not using MWB
    "Proxies"
    {
        "MwEnvMapTint"
        {
            "specularity" "0.5"                             //0.25, 0.5, 0.75, 1
            "min" "0"
            "max" "0.2"                                     //change this if too bright in game
        }
    }
}